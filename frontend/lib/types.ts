import type { User } from "@/lib/auth";
export type PledgeOption = { id:number; title:string; description?:string|null; amountEth:string; sortOrder:number };
export type Milestone = { id:number; title:string; amountEth:string; sortOrder:number; evidenceSubmitted?:boolean; evidenceUri?:string|null; voteStart?:string|null; voteEnd?:string|null; yesVotesEth?:string; noVotesEth?:string; released?:boolean };
export type Post = { id:number; projectId:number; userId:number; content:string; createdAt:string; updatedAt?:string; user?:User };
export type Project = { id:number; userId?:number|null; title:string; description:string; imageUrl?:string|null; creatorAddress:string; contractAddress?:string|null; chainId:number; goalEth:string; totalFundedEth?:string; currentMilestone?:number; status?:string; fundingDeadline?:string|null; votingDurationSec?:number|null; quorumBps?:number; yesThresholdBps?:number; authSignerAddress?:string|null; milestones:Milestone[]; pledgeOptions:PledgeOption[]; posts?:Post[] };
export type ChainMilestone = { amount:string; pledgedAmount:string; fundingClosed:boolean; evidenceSubmitted:boolean; evidenceURI:string; voteStart:number; voteEnd:number; yesVotes:string; noVotes:string; voteBaseAmount:string; released:boolean };
export type ChainState = { owner?:string; goalAmount:string; totalFunded:string; currentMilestone:number; fundingDeadline:number; myPledge:string; quorumBps?:number; yesThresholdBps?:number; milestones:ChainMilestone[]; myMilestonePledge?:string; currentMilestoneFunded?:boolean; currentMilestoneFundingClosed?:boolean };
