# Transparent Crowdfund Frontend Next16 UI

기존 v4 백엔드/컨트랙트와 연동되는 UI 개선 프론트엔드입니다.

## 실행
```bash
pnpm install
copy .env.local.example .env.local
pnpm dev
```

`.env.local`의 `NEXT_PUBLIC_FACTORY_ADDRESS`에 배포된 Factory 주소를 입력하세요.
